# Getting Started

This project was bootstrapped with [Express NodeJS](https://expressjs.com/).

## Available Scripts

In the project directory, you can run:

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:5000](http://localhost:5000) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.

## The project uses mongoDB with mongoose.

You need to create a new table in mongoDB and add url into env file

To learn Mongoose, check out the [Mongoose documentation](https://mongoosejs.com/).